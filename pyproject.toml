[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "medicobuddy"
version = "0.1.0"
description = "Safety-first, evidence-grounded GraphRAG wellness assistant"
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.11"
dependencies = [
    # Web framework
    "fastapi>=0.115.0,<1.0",
    "uvicorn[standard]>=0.32.0,<1.0",
    "pydantic>=2.10.0,<3.0",
    "pydantic-settings>=2.7.0,<3.0",
    # Streamlit frontend
    "streamlit>=1.41.0,<2.0",
    # LangGraph / LangChain
    "langgraph>=0.4.0,<1.0",
    "langchain>=0.3.0,<1.0",
    "langchain-groq>=0.2.0,<1.0",
    "langchain-community>=0.3.0,<1.0",
    # Databases (pgvector + Neo4j only — no Milvus, no Redis)
    "neo4j>=5.27.0,<6.0",
    "pgvector>=0.3.0,<1.0",
    "asyncpg>=0.30.0,<1.0",
    "sqlalchemy[asyncio]>=2.0.36,<3.0",
    # Embeddings & Transformers (Qwen3-Embedding-0.6B local)
    "sentence-transformers>=3.4.0,<4.0",
    "transformers>=4.48.0,<5.0",
    "torch>=2.2.0",
    # PDF parsing
    "PyMuPDF>=1.25.0,<2.0",
    # HTTP clients
    "httpx>=0.28.0,<1.0",
    "aiohttp>=3.11.0,<4.0",
    # XML parsing
    "lxml>=5.3.0,<6.0",
    # Observability
    "structlog>=24.4.0,<25.0",
    # Multipart form support
    "python-multipart>=0.0.18",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0,<9.0",
    "pytest-asyncio>=0.25.0,<1.0",
    "pytest-cov>=6.0.0,<7.0",
    "pytest-httpx>=0.35.0,<1.0",
    "ruff>=0.9.0,<1.0",
    "mypy>=1.14.0,<2.0",
    "bandit>=1.8.0,<2.0",
    "pre-commit>=4.0.0,<5.0",
    "httpx>=0.28.0,<1.0",
    "pytest-mock>=3.14.0,<4.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/medicobuddy"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
asyncio_mode = "auto"
markers = [
    "safety: marks tests as safety-critical (deselect with '-m \"not safety\"')",
    "adversarial: marks adversarial prompt injection tests",
    "integration: marks integration tests requiring running services",
]
